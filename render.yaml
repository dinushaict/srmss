# Render.com deployment config for SRMSS.
# Lets Render build and run the app automatically from this repo.
services:
  - type: web
    name: srmss
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
